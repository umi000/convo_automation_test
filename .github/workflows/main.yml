name: Run Tests

on:
  repository_dispatch:
    types: [admin, player]

jobs:
  run_tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install Dependencies
        run: npm install

      - name: Run Tests
        run: |
          if [[ "${{ github.event.action }}" == "admin" ]]; then
            echo "Running admin tests..."
            npx cypress run
          elif [[ "${{ github.event.action }}" == "player" ]]; then
            echo "Running player tests..."
            npx cypress run

            
          fi
      - name: Upload Cypress Artifacts
        uses: actions/upload-artifact@v3
        with:
          name: Cypress-Test-Artifacts
          path: cypress/videos
